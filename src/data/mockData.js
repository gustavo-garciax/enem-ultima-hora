import db from './db.json';

export const userData = db.user;
export const user = db.user;

export const subjectsData = db.subjects;
export const subjects = db.subjects;

export const dashboardStats = db.dashboardStats;
export const featuredStudy = db.featuredStudy;

export const nextStudiesList = db.nextStudies;
export const nextStudies = db.nextStudies;

export const weeklyGoal = db.weeklyGoal;
export const progressStats = db.progressStats;
export const weeklyProgress = db.weeklyProgress;
export const overallProgress = db.overallProgress;

export const calendarEvents = db.calendarActivities;
export const calendarActivities = db.calendarActivities;

export default db;
